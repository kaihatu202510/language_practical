<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.model.mapper.SkillMapper">
	<select id="getSkills" resultType="com.example.demo.model.dto.SkillView">
		SELECT s.id, s.user_id AS "userId", u.name AS "userName", s.name AS "skill"
		FROM skills s
		JOIN users u
		ON s.user_id = u.id		
		WHERE u.enabled = true
	</select>
	
	<!--IDでスキル取得-->
	<select id="getSkill" resultType="com.example.demo.model.entity.Skill">
		SELECT id, user_id AS "userId", name
		FROM skills
		WHERE id = #{id}
	</select>
	
	<!--挿入-->
	<insert id="insertSkill" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO skills(user_id, name)
		VALUES(#{userId}, #{name})
	</insert>
	
	<!--物理削除-->
	<delete id="deleteSkill">
	    DELETE FROM skills
	    WHERE id = #{id}
	</delete>
	
	<!--更新-->
	<update id="updateSkill">
		UPDATE skills
		SET name = #{name}
		WHERE id = #{id}
	</update>
</mapper>