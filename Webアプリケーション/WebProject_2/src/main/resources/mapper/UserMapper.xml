<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.model.mapper.UserMapper">
	<select id="findByName" resultType="com.example.demo.model.entity.User">
		SELECT * FROM users
		WHERE name = #{name}
	</select>
	
	<select id="findUserById" resultType="com.example.demo.model.entity.User">
		SELECT * FROM users
		WHERE id = #{id}
	</select>
	
	<!--ユーザー全行取得-->
	<select id="getUsers" resultType="com.example.demo.model.entity.User">
		SELECT * FROM users
		<where>
			enabled = true
			<if test="keyword != null and keyword != ''">
				AND name LIKE CONCAT('%', #{keyword}, '%')
			</if>
		</where>
		ORDER BY id ASC
	</select>
	
	<!--ユーザー挿入-->
	<insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO users(name, password, enabled, role)
		VALUES(#{name}, #{password}, #{enabled}, #{role})
	</insert>
	
	<!--ユーザー論理削除-->
	<update id="deleteUser">
	    UPDATE users
	    SET enabled = false
	    WHERE id = #{id}
	</update>
</mapper>